import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#232f3e",
    },
    secondary: {
      main: "#FF9900",
    },
    white: {
      main: "#fff",
    },
  },
});

export default theme;
