import styled from "styled-components";
import Grid from "@material-ui/core/Grid";

export const CartPageSection = styled.div`
  min-height: 80vh;
  padding-top: 3rem;
`;

export const GridItem = styled(Grid)`
  align-self: center;
`;
